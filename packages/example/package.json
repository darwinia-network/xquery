{
  "name": "xquery-example",
  "version": "0.1.10",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "init:db": "npx prisma migrate dev --name init",
    "generate:prisma": "npx prisma generate --schema=./prisma/schema.prisma",
    "start:docker": "docker-compose pull && docker-compose up"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@darwinia/xquery-type": "^0.1.10",
    "@prisma/client": "~4.2.0",
    "axios": "^0.27.2",
    "prisma": "~4.2.0",
    "typescript": "^4.1.3"
  }
}
